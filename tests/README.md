# Tests

This directory contains tests for the Relay reference implementation.

The baseline includes:
- Contract and schema validation
- Integration tests
- Load and rate-limit behavior tests

Tests may be language-agnostic or ecosystem-specific,
depending on their purpose.
